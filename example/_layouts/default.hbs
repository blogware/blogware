<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{#if @page.title}}{{@page.title}}{{else}}{{@site.title}}{{/if}}</title>
  </head>
  <body>
    {{> header}}

    {{{body}}}
  </body>
</html>
